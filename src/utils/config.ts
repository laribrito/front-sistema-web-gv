import { stringAleatoria } from "./functions";

// config.js
const config = {
    WIDTH_WIDGETS: "300px",
    WIDTH_HALF_WIDGETS: "150px",
    SECRET_KEY_CRIPT: stringAleatoria(150)
  };
  
  export default config;